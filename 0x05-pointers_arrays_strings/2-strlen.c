#include "holberton.h"

/**
	* _strlen - updates the value it points to to 98.
	* @s: a variable as referencia
	* followed by a new line.
	* Return: none.
	*/

int _strlen(char *s)
{
	int i;

	for (; s[i] != 0 ; i++)
	{
	}

	return (i);
}
