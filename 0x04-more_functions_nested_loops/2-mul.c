#include "holberton.h"

/**
 * mul -  function that multiplies two integers.
 * @a: The number to multiplie
 * @b: The number to multiplie
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int mul(int a, int b)
{
	return (a * b);

}
